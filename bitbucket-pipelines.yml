image: douglatornell/salishsea:nemo-cmd-test

pipelines:
  default:
    - step:
        name: pytest package
        script:
          - /opt/conda/envs/nemo-cmd-test/bin/coverage run -m pytest --junitxml=./test-reports/junit.xml
          - /opt/conda/envs/nemo-cmd-test/bin/coverage report
